<Window x:Class="FlagInjectorWpf.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Icon="Assets/FlagBrowser.ico"
        Width="560"
        Height="690"
        MinWidth="520"
        MinHeight="580"
        WindowStartupLocation="CenterOwner"
        Background="#080B12">
    <Window.Resources>
        <SolidColorBrush x:Key="Panel" Color="#0E1420"/>
        <SolidColorBrush x:Key="Panel2" Color="#121B2A"/>
        <SolidColorBrush x:Key="Border" Color="#25354F"/>
        <SolidColorBrush x:Key="Text" Color="#EEF4FF"/>
        <SolidColorBrush x:Key="Muted" Color="#91A1BA"/>
        <SolidColorBrush x:Key="Accent" Color="#3B82F6"/>
        <SolidColorBrush x:Key="Green" Color="#22C55E"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border Background="Transparent" Padding="0,2">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Grid Width="16" Height="16" Margin="0,0,8,0">
                                    <Border x:Name="Box"
                                            Background="{StaticResource Panel2}"
                                            BorderBrush="{StaticResource Border}"
                                            BorderThickness="1"
                                            CornerRadius="4"/>
                                    <Path x:Name="Mark"
                                          Data="M 3 8 L 6.5 11.5 L 13 4.5"
                                          Stroke="White"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Visibility="Collapsed"/>
                                </Grid>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource Accent}"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="#60A5FA"/>
                                <Setter TargetName="Mark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value=".55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource Panel2}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="{StaticResource Accent}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Panel2}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ScrollBarLineButton" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="Thumb"
                                Background="#2E4262"
                                CornerRadius="5"
                                Margin="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Thumb" Property="Background" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="Thumb" Property="Background" Value="#60A5FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="#0B111C"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Root" Width="10" Background="#0B111C">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.PageUpCommand"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.PageDownCommand"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="Root" Property="Width" Value="Auto"/>
                                <Setter TargetName="Root" Property="Height" Value="10"/>
                                <Setter TargetName="PART_Track" Property="Orientation" Value="Horizontal"/>
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#0B111C" BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Padding="18,14">
            <StackPanel>
                <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="Runtime and apply behavior" Foreground="{StaticResource Muted}" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="18">
                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Admin Status" FontWeight="SemiBold"/>
                        <TextBlock x:Name="ElevationText" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Compatibility" FontWeight="SemiBold"/>
                        <CheckBox x:Name="SafeModeBox" Content="Enable compatibility mode"/>
                        <TextBlock Text="Uses the compatibility path." Foreground="{StaticResource Muted}" FontSize="12" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Re-apply" FontWeight="SemiBold"/>
                        <CheckBox x:Name="ReApplyBox" Content="Re-apply flags at fixed interval"/>
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Interval (ms)" Foreground="{StaticResource Muted}" VerticalAlignment="Center"/>
                            <TextBox x:Name="ReApplyIntervalBox" Grid.Column="1"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Random Re-apply" FontWeight="SemiBold"/>
                        <CheckBox x:Name="RandomReApplyBox" Content="Re-apply at random intervals"/>
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="22"/>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Min (ms)" Foreground="{StaticResource Muted}" VerticalAlignment="Center"/>
                            <TextBox x:Name="RandomMinBox" Grid.Column="1"/>
                            <TextBlock Grid.Column="3" Text="Max (ms)" Foreground="{StaticResource Muted}" VerticalAlignment="Center"/>
                            <TextBox x:Name="RandomMaxBox" Grid.Column="4"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Early Apply [BETA]" Foreground="#F59E0B" FontWeight="SemiBold"/>
                        <CheckBox x:Name="TimingAttackBox" Content="Apply earlier after attach"/>
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Delay after attach (ms)" Foreground="{StaticResource Muted}" VerticalAlignment="Center"/>
                            <TextBox x:Name="TimingDelayBox" Grid.Column="1"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Alternate Paths" FontWeight="SemiBold"/>
                        <CheckBox x:Name="OffsetlessBox" Content="Enable alternate lookup"/>
                        <TextBlock Text="Enable automatic secondary lookup" Foreground="{StaticResource Muted}" FontSize="12" Margin="24,2,0,0"/>
                        <CheckBox x:Name="DiskFallbackBox" Content="Enable disk backup"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Recording And Order" FontWeight="SemiBold"/>
                        <CheckBox x:Name="StealthBox" Content="Pause applies while recording"/>
                        <CheckBox x:Name="ShuffleBox" Content="Shuffle apply order"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Batch Settings" FontWeight="SemiBold"/>
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="22"/>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Batch size" Foreground="{StaticResource Muted}" VerticalAlignment="Center"/>
                            <TextBox x:Name="BatchSizeBox" Grid.Column="1"/>
                            <TextBlock Grid.Column="3" Text="Batch delay (ms)" Foreground="{StaticResource Muted}" VerticalAlignment="Center"/>
                            <TextBox x:Name="BatchDelayBox" Grid.Column="4"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Background="#0B111C" BorderBrush="{StaticResource Border}" BorderThickness="0,1,0,0" Padding="14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="92" Click="Cancel_Click"/>
                <Button Content="Save" Width="92" Margin="8,0,0,0" Background="{StaticResource Green}" Click="Save_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
